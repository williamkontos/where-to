<div class="container">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
      <h2 class="text-center">Make a list of restaurants for your peers to vote on!</h2>
      <%= bootstrap_form_with(model: poll) do |f| %>

          <%#= f.text_field :restaurant, 
                            autofocus: true, 
                            class: 'form-control', 
                            placeholder: 'restaurant' %>

        <%= f.submit "Create Poll", class: 'form-control btn btn-primary' %>
      <% end %>            
    </div>
  </div> 
</div>

